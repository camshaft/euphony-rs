[package]
name = "euphony"
version = "0.1.0"
authors = ["Cameron Bytheway <bytheway.cameron@gmail.com>"]
edition = "2018"

[features]
default = ["runtime", "std"]
std = []
runtime = ["std"]

[dependencies]
num-rational = { version = "0.2", default-features = false }
