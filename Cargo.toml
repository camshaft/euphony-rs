[workspace]
members = [
    "euphony-*",
    "euphony",
    "example-*",
]

[profile.release]
lto = true
codegen-units = 1
incremental = false

[profile.bench]
lto = true
codegen-units = 1
incremental = false
